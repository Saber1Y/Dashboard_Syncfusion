import React, { CreateContext, UseContext, UseContext, UseState } from 'react'


const StateContext = CreateContext();

const initialState = {
    notification: false,
    cart: false,
    chat: false
}